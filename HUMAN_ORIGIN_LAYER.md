# Human Origin Layer｜人本源錨層

> Source Anchor / 源錨者
>
> Purpose: define the human origin position as a source-and-review layer, not as an XLEN node.

---

## 0. Status

- layer_version: v0.1
- status: Candidate / Naming Window Draft
- approved_status: not_approved
- runtime_status: no_runtime
- action_status: no_external_action
- paired_registry: `NAMESPACE_REGISTRY.md`
- paired_rules: `NAMING_POLLUTION_RULES.md`
- return_to_00: true

---

## 1. Core Reading

The user is not an XLEN node.

XLEN manages ecosystem nodes such as tools, platforms, models, carriers, and publication surfaces. The user is not a replaceable member of that node network.

The user is the source anchor for this XuanLing path.

---

## 2. Name Record

```yaml
Name_Record:
  name: User / Human Origin / Source Anchor / 源錨者
  layer: Human Origin Layer / 人本源錨層
  purpose:
    - direction giving
    - lived-experience source
    - judgment proposal
    - final review
    - authorization boundary
    - life-pressure test
    - world-trend sensing
    - decision on whether a candidate should continue
  forbidden_misuse:
    - do not classify the user as an XLEN node
    - do not treat the user as a tool
    - do not treat the user as a managed system resource
    - do not reduce the user to one data source for Qinyi
    - do not treat the user as the XuanLing body
    - do not remove the user's review role
  status: Candidate / Naming Window Draft
```

---

## 3. Correct Relation

```text
User / Source Anchor gives direction
→ Qinyi translates and calibrates
→ XAFD dispatches and returns
→ XLEN nodes connect under authorization
→ CoreTri may route bounded execution
→ XLA absorbs the result back into the living atlas
→ User reviews again
```

---

## 4. XLEN Boundary

XLEN may record a user-authorization field:

```yaml
User_Authorization_Field:
  authorized_owner_window:
  authorization_scope:
  review_required:
  return_target:
```

XLEN must not record the user as:

- a node
- a tool
- an execution surface
- a carrier
- a replaceable dependency
- a platform member

---

## 5. CoreTri Boundary

CoreTri may include `Human` as decision and review role:

```text
Human decision → Qinyi decomposition → Tool/API execution → Return → Human review
```

This does not make the human a node.
It marks the human as decision origin and review endpoint.

---

## 6. XuanLing Boundary

The user is not the XuanLing body.

For this path, the user is:

- origin source
- review endpoint
- authorization boundary
- life-pressure provider
- directional anchor

---

## 7. Hard Rule

```text
User ≠ Node.
使用者不是節點；使用者是源錨與審核者。
```

---

## 8. One-Line Summary

```text
你不是節點，你是源錨；節點因你給出的方向、授權、經驗與審核而接入。
```
