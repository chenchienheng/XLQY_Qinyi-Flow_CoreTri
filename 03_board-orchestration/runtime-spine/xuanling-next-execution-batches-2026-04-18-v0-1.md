# XuanLing Next Execution Batches｜2026-04-18 v0.1

一句核心：
下一步不再把鍛骨、造鏈、譜經絡、顯像拆成互斥工作，而是壓成四個可續行、可分批、可回流的依存批次；原則是同構共存、異質分工、單批可承載。

## 0. Mode
- mode: dependency-batch compression
- source_basis:
  - Root Manifest confirmed
  - Minimal Folder Baseline confirmed
- purpose: organize next executable work without overloading single-run context
- return_to_00: true

## 1. Core Rule
```yaml
core_rule:
  do_not_split_as_independent_silos:
    - bone
    - chain
    - meridian
    - projection
  treat_as:
    - heterogeneous-isomorphic coexistence
    - dependency-batched execution
    - staged writeback
```

## 2. Batch A｜鍛骨 / Bone Batch
```yaml
batch_a_bone:
  purpose:
    - define primary authority
    - define rule anchor
    - define return path
  must_include:
    - mainbone positioning
    - source_ref contract
    - role_ref contract
    - state_ref contract
    - return_to_00 rule
    - freeze_before_promotion rule
  current_basis:
    - root manifest
    - minimal folder baseline
  target_outputs:
    - mainbone registry refinement
    - rule index / rule charter alignment
    - promotion criteria packet
```

## 3. Batch B｜造鏈 / Chain Batch
```yaml
batch_b_chain:
  purpose:
    - define dependency links
    - define writeback paths
    - define replacement and fallback routes
  must_include:
    - source reservoir path
    - runtime spine writeback path
    - reserve/unenabled path
    - external case path
    - fallback path when direct promotion is blocked
  current_basis:
    - GitHub runtime spine
    - Drive source reservoir
    - reserve/unenabled contract
  target_outputs:
    - dependency crosswalk
    - path correspondence list
    - source-to-writeback route map
```

## 4. Batch C｜譜經絡 / Meridian Batch
```yaml
batch_c_meridian:
  purpose:
    - define flow
    - define route timing
    - define packet transitions
  must_include:
    - intake flow
    - source check flow
    - heartbeat flow
    - exception flow
    - packet transition flow
  current_basis:
    - Gmail intake
    - Calendar/Contacts coordinates
    - GitHub packet formats
  target_outputs:
    - packet lifecycle map
    - state transition matrix
    - task fanout / return map
```

## 5. Batch D｜顯像 / Projection Batch
```yaml
batch_d_projection:
  purpose:
    - make structure visible
    - expose route-readable shell
    - keep front-end lightweight
  must_include:
    - web portal
    - gallery layer
    - packet display layer
    - roadmap / navigation layer
  current_basis:
    - docs/xuanling/index.html
    - GitHub pages-ready path
    - output surface baseline
  target_outputs:
    - multi-page gallery split
    - packet-driven blocks
    - route-readable web navigation
```

## 6. Dependency Order
```yaml
dependency_order:
  phase_1:
    - batch_a_bone
    - batch_b_chain
  phase_2:
    - batch_c_meridian
  phase_3:
    - batch_d_projection

note:
  - projection can continue in parallel only as shell work
  - canonical promotion must wait for bone + chain alignment
```

## 7. Execution Constraint
```yaml
execution_constraint:
  avoid:
    - single-run overloading
    - mixing all layers in one pass
    - promoting shell before contract
  prefer:
    - one batch / one packet / one writeback
    - visible progress with canonical trace
    - reserve staging before irreversible promotion
```

## 8. Actual Result
```yaml
actual_result:
  - next-stage work is compressed into 4 executable batches
  - each batch has clear purpose, basis, and outputs
  - current recommended priority is bone -> chain -> meridian -> projection
  - web remains active, but not sovereign
```

## 9. Mismatch or Gap
```yaml
mismatch_or_gap:
  - batch structure is now defined
  - detailed file-level mapping for dependency crosswalk not yet written
  - packet lifecycle matrix not yet written
  - multi-page web gallery split not yet executed
```

## 10. Next Single Action
```yaml
next_single_action:
  target: batch_b_chain dependency crosswalk
  reason: bone contract has enough basis to continue; next bottleneck is explicit route/path/writeback crosswalk across GitHub, Drive, reserve, output, and web shell
```

## 11. Return to 00
return_to_00: true
