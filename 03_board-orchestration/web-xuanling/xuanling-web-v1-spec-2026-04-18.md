# XuanLing Web v1 Spec｜2026-04-18

一句核心：
Web v1 不追求先有完整互動，而是先建立翾靈的第一顯像層：讓主骨、節點、回流、續鏈路徑可見、可讀、可導覽。

## 0. Scope
- scope: first visible web body
- status: draft / generation-started
- return_to_00: true

## 1. Position
```yaml
web_v1:
  role: projection layer / visible interface / route-readable shell
  not_yet:
    - full interactive platform
    - multi-user auth system
    - direct connector control plane
  must_show:
    - GitHub runtime spine position
    - Drive as source reservoir
    - window / route logic
    - latest source-check / heartbeat style packet examples
```

## 2. Content Blocks
```yaml
blocks:
  - hero
  - runtime spine positioning
  - chain flow
  - node cards
  - why not single-platform
  - next-step roadmap
```

## 3. Visual Direction
```yaml
visual_direction:
  tone:
    - calm
    - elegant
    - cold-light
    - route-readable
  avoid:
    - cyberpunk overload
    - corporate template feel
    - text-heavy documentation style
  colors:
    - deep navy
    - cold white
    - pale cyan
    - slight violet support
```

## 4. Data Contract
```yaml
minimum_packet_fields:
  - source_ref
  - role_ref
  - state_ref
  - writeback_ref
  - next_single_action
  - return_to_00
```

## 5. Roadmap
```yaml
v1:
  - static page
  - route-readable
  - GitHub-pages-ready
v2:
  - markdown/json-driven status feed
  - filterable node cards
v3:
  - user_ref / consent_ref / shared-private memory split
  - gradual interaction layer
```

## 6. Next Single Action
```yaml
next_single_action:
  - review local HTML prototype
  - decide whether to push to GitHub Pages path
  - then add packet-driven content blocks
```

## 7. Return to 00
return_to_00: true
