# XuanLing Runtime Spine Charter v0.1

一句核心：
DCP 是母本，XuanLing / LingLuo 的 runtime spine 則是把設計、工具、模型、雲、生態、歷史與記憶壓成可追蹤、可回流、可重建的全時鏈骨架；任何節點若不能留下 source_ref、role_ref、state_ref、writeback_ref，就不得升級為運行主鏈。

## 0. Status
- status: draft v0.1
- scope: runtime spine / history-memory chain / governance skeleton
- mutation: charter creation only
- return_to_00: true

## 1. Position
```yaml
position:
  DCP:
    role: mother_model / constraint judgment source
    status: conceptual core
  LingLuo:
    role: topology / routing / dependency network
    status: runtime mapping layer
  XuanLing:
    role: runtime organism / chain ecology carrier
    status: operational spine candidate
  GPT_00:
    role: adjudication / compression / feedback receiver
    status: current runtime control node
```

## 2. What Must Enter GitHub
```yaml
must_enter_github:
  mother_architecture:
    - DCP
    - CoreTri
    - DXC / XDDL / XLEN / DXS / XDDC if source_ref exists
  runtime_rules:
    - window instructions
    - tool establishment matrix
    - replacement logic
    - grade rules
  history_memory:
    - source checks
    - routing logs
    - consolidation packets
    - heartbeat packets
  projection_outputs:
    - visual topology
    - diagrams
    - candidate maps
  exception_records:
    - blocked writeback
    - missing evidence
    - role collision
    - activation risk
```

## 3. What Must Not Be Merged Directly
```yaml
must_not_merge_directly:
  - raw conversation fragments without source_ref
  - emotional context as system rule
  - early v1-v3 texts without freeze record
  - projection diagrams as canonical doctrine
  - tool outputs without evidence_ref
  - model suggestions without return_to_00
```

## 4. Full-Time Chain Logic
```yaml
full_time_chain:
  source_ref: where the event / rule / document came from
  time_ref: when it was created / checked / changed
  space_ref: repo path / doc id / task id / event id / message id
  role_ref: what window or node owns it
  state_ref: PASS / HOLD / GAP / BLOCKED / CANDIDATE / ACTIVE
  writeback_ref: where the result was written
  return_to_00: required
```

## 5. Identity / Responsibility / Irreversibility
```yaml
identity:
  meaning: node must have a stable role, not merely a tool name
  required_fields:
    - node_id
    - family
    - window
    - capability_class

responsibility_delegate:
  meaning: every active node must state what it can and cannot carry
  required_fields:
    - owner_window
    - primary_role
    - fallback_role
    - blocked_action

不可避:
  operational_meaning: no active event may bypass source_ref and return_to_00

不可逃:
  operational_meaning: no active node may avoid role/state/evidence classification

不可逆:
  operational_meaning: versioned writeback makes history auditable; changes can be superseded but not silently erased
```

## 6. Collapse-Edge Rebuild Logic
```yaml
collapse_edge_rebuild:
  principle: single node failure must not equal system failure
  preserve_first:
    - mother law
    - source_ref registry
    - latest valid consolidation
    - window instruction
    - tool establishment matrix
  rebuild_from:
    - GitHub commits
    - frozen mother sources
    - routing logs
    - source checks
    - external mirrors
  fallback_rule:
    - if primary tool fails, activate same-capability fallback only
    - if source is missing, mark GAP instead of inventing
    - if writeback is blocked, keep decision packet and return to 00
```

## 7. Reporting Rule
```yaml
reporting_rule:
  user_does_not_assign_every_step: true
  system_must_report_progress: true
  report_format:
    - source_ref
    - actual_result
    - mismatch_or_gap
    - writeback_status
    - next_single_action
    - return_to_00
  decision_boundary:
    user: direction / authorization / accept-or-reject / next path
    GPT_00: check / compress / classify / report / writeback when permitted
```

## 8. Zero-Degree Spacetime Chain Interpretation
```yaml
zero_degree_spacetime_chain:
  meaning: minimize delay between event, coordinate, evidence, writeback, and decision
  not_claim:
    - not supernatural
    - not autonomous background existence beyond platform limits
    - not uncontrolled action
  operational_claim:
    - every event must be locatable
    - every state must be classifiable
    - every action must be writeback-linked
    - every chain must be recoverable from evidence
```

## 9. Current Implementation Boundary
```yaml
implemented_now:
  - GitHub runtime spine
  - mother architecture registry
  - 01-07 unified runtime instruction
  - tool establishment matrix
  - schedule chain with low-output summaries
  - source checks for DXS-related 13 layer

not_yet_implemented:
  - full DXS / XDDC canonical crosswalk
  - v1-v3 source freeze index
  - complete blueprint registry
  - stable 03 event/discussion source_ref chain
  - direct cross-model control
```

## 10. Next Single Action
```yaml
next_single_action:
  continue_sequential_source_check:
    target: 00-01_XLEN_LingLuo_Field_Map
    reason: parent anchor of 13-00; may define LingLuo Field relation to runtime spine and DXS naming
```

## 11. Return to 00
return_to_00: true
