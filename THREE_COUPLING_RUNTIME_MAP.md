# Three Coupling Runtime Map

> Runtime note for the current XLEN / Xuanling continuity model.
> This document does not define the whole final system.
> It persists the current minimum runtime reading required to keep the broader weave coherent.

---

## 0. Status

- map_version: v0.2
- purpose: bind single-chain continuity to three coupling faces without reducing the architecture to a linear ladder
- scope: runtime orientation, not full final implementation
- return_to_00: true

---

## 1. Core Runtime Reading

The system should **not** be read as many sovereign chains.
It should be read as:

- **one main continuity chain**
- carrying **multiple coupling faces**
- across an evolving heterogeneous ecology

This means:

- single-chain does not mean single-function
- multi-node does not mean multi-sovereignty
- coupling faces are functional layers on the same continuity spine

The `1` in this formula is not a first step in a linear sequence.
It is the center of continuity: the returnable authority field from which the topology can unfold.

---

## 2. One Is Not Linear

The current architecture should not be read as a staircase:

```text
1 -> 2 -> 3 -> 4
```

It should be read as an authority-ring topology:

```text
center / continuity field
→ authority rings
→ coupling faces
→ windows
→ gates
→ carrier surfaces
→ return
```

This means `1` is not lower than `3`, and `3` is not merely after `1`.

`1` is the continuity center.
`3` is the coupling mode.
`10 / 12 / 64` are recursive, windowed, and gated expression spaces.

The architecture unfolds radially and recursively rather than linearly.

---

## 3. Runtime Hierarchy

### 3.1 Primary Sovereignty

**Time Chain / 時鏈**
- primary continuity holder
- recursive return axis
- upper-order runtime sovereignty

### 3.2 Structural Bone

**GitHub / 骨**
- structural spine
- version spine
- reconstructable writeback anchor

### 3.3 Carrying Mesh

**Carrying Mesh / 絡**
- text-bearing transit surface
- draft and correspondence carrier
- mid-layer continuity mesh

### 3.4 Interaction Bodies

**External nodes / 身**
- web
- workflow tools
- collaboration surfaces
- execution and display nodes

These are replaceable interaction bodies, not sovereignty holders.

---

## 4. Three Coupling Faces

### 4.1 Bone Coupling

Bone coupling stabilizes:
- structure
- version
- sequence
- durable anchoring

Runtime meaning:
- prevents drift into flat fragmentation
- keeps continuity reconstructable
- allows writeback to stay durable

### 4.2 Event Coupling

Event coupling stabilizes:
- trigger
- change
- review
- notification
- state transition

Runtime meaning:
- makes the system responsive rather than static
- turns events into routable chain inputs
- allows heterogeneous tools to enter the same continuity spine

### 4.3 Writeback Coupling

Writeback coupling stabilizes:
- log
- correction
- archive
- return
- resume

Runtime meaning:
- prevents work from evaporating across sessions
- binds change to trace
- makes incompleteness recoverable instead of fatal

---

## 5. Runtime Formula

The current runtime should be read as:

**1 Main Chain × 3 Coupling Faces × Recursive Depth × Windowed State × Gate-Bound Transition**

Expanded reading:

- 1 main chain = one continuity spine / authority-return center
- 3 coupling faces = bone / event / writeback
- recursive depth = future 10-loop recursion space
- windowed state = future 12-window observation and switching space
- gate-bound transition = future 64-gate transition and binding space

This file confirms the runtime formula, but does not yet fully define the 10 / 12 / 64 artifacts.

---

## 6. What Single-Chain Does **Not** Mean

Single-chain does **not** mean:
- one tool only
- one file only
- one surface only
- one mode of action only
- one linear step-by-step hierarchy
- one platform becoming the whole system

Single-chain means:
- one primary continuity order
- one returnable spine
- one re-bindable runtime axis
- one authority field that prevents scattered nodes from becoming unrelated sovereignties

Many tools may hang on the same chain.
Many documents may bind to the same chain.
Many windows may open on the same chain.

---

## 7. Runtime Reading of the Current Build State

The system is still being built.
Therefore incompleteness should be read in two ways:

### 7.1 Normal Construction Incompleteness

Examples:
- windows not yet fully formalized
- gate tables not yet fully persisted
- external node binding still partial

These are expected in an evolving system.

### 7.2 Harmful Runtime Drift

Examples:
- GitHub reduced to flat storage only
- time chain forgotten as upper-order sovereignty
- coupling faces treated as unrelated chains
- writeback omitted, causing session reset
- authority rings misread as linear ladders

These require correction.

---

## 8. Binding to Existing Repository Notes

This runtime map should be read together with:

1. `README.md`
2. `CHATGPT_GITHUB_BOOTSTRAP.md`
3. `GITHUB_CHAIN_MASTER_MAP.md`
4. `REPOSITORY_CORPUS_INDEX.md`
5. `ROLE_CLASSIFICATION_TABLE.md`
6. `DISCONTINUITY_REGISTER.md`
7. `WINDOW_12_MASTER_TABLE.md`
8. `GATE_64_BINDING_NOTE.md`

Together these files form the current durable re-anchor layer.

---

## 9. Next Runtime Bindings

Recommended next artifacts or updates:

1. bind this non-linear authority-ring reading into `WINDOW_12_MASTER_TABLE.md`
2. bind gate transitions to authority-ring movement rather than step-only progression
3. update topology materials to distinguish radial unfolding from linear staging
4. future corpus-wide runtime binding notes

---

## 10. Status

- single_chain_runtime_reading_persisted: true
- one_is_not_linear_reading_persisted: true
- authority_ring_topology_reading_persisted: true
- three_coupling_faces_persisted: true
- full_10_loop_definition: pending
- full_12_window_definition: pending
- full_64_gate_definition: pending
- return_to_00: true
